Generate ActionableBiologyOutput.

The artifact_id must be exactly:
{planned_artifact_id}

Payload JSON:
{payload_json}

Convert current tumor state and report biomarkers into actionable_biology rows. Use actionability categories only from the schema. Separate evidence-supported biology from insufficient-evidence biology. Put unresolved or missing support in unresolved_evidence.
