Generate ClaimEvidenceListOutput for the Translume MVP.

The artifact_id must be exactly:
{planned_artifact_id}

Payload JSON:
{payload_json}

Classify claims as patient-specific findings, graph/tool/Medea-supported context, model-derived hypotheses, missing evidence, or conflicting evidence. Unsupported claims must remain needs_review.
