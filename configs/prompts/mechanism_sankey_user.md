Generate MechanismSankeyOutput for the Translume MVP.

The artifact_id must be exactly:
{planned_artifact_id}

Payload JSON:
{payload_json}

Build a Finding -> Mechanism -> Molecular Fit -> Validation Test graph. Link values are visual weights, not probabilities. Node IDs must be unique.
Keep the graph compact: at most 8 nodes and 10 links. Prefer one direct reviewable path per supported molecular finding. Do not expand literature, graph, or tool evidence into extra nodes unless it is needed to connect the path.
