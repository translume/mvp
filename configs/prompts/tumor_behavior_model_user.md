Generate TumorBehaviorModelOutput for the Translume MVP.

The artifact_id must be exactly:
{planned_artifact_id}

Payload JSON:
{payload_json}

Use only hypothesis-generating tumor-state language. Do not generate transition probabilities, treatment recommendations, or outcome predictions.
