Generate CurrentTumorStateOutput.

The artifact_id must be exactly:
{planned_artifact_id}

Payload JSON:
{payload_json}

Identify current tumor drivers, active pathways, co-drivers, actionable/uncertain alterations, immune and DNA-repair context, and missing data. Cite source_artifact_ids from the payload for every current_tumor_state object.
