[project]
name = "worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ['fastapi', 'uvicorn', 'pydantic']

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/translume_worker"]
