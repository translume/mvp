[project]
name = "medea-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "httpx",
    "numpy>=1.24.0",
    "h5py>=3.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
