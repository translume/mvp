[project]
name = "translume-ui"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ['gradio>=6.0.0,<7.0.0', 'httpx', 'pydantic', 'plotly>=6.0.0']

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
