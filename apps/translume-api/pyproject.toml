[project]
name = "translume-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ['fastapi', 'uvicorn', 'python-multipart']

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
