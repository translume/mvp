[project]
name = "translume-adapters"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ['polars>=1.0.0', 'httpx', 'pydantic']

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
